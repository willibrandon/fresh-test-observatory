name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm install --global @fresh-editor/fresh-editor@0.4.10
      - run: npm ci
      - run: npm run validate
      - run: fresh --cmd script types
      - run: npm run check-types
